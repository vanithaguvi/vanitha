#include<stdio.h>
int main(void)
{
int i,c,n,a=1,b=10;
printf("\n enter the number");
scanf("%d",&n);
  if(a<b)
  {
for(i=1;i<=n;i++);
{
c++;
}
  }
if(c==2)
{
printf("\n given no.is a prime no");
}
else
{
printf("\n ginen no. is not a prime no.");
}
}
